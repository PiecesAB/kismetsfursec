What to do after building from Unity:

- Create SFX archive using WinRAR
- select the files inside, not the folder. or else the SFX will be unable to find the game .exe

- Use resource hacker to modify the manifest to change the app's run level to "requireAdministrator" instead of "asInvoker".