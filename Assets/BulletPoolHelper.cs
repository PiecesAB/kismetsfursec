using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// it just seems to hold the bullet pool
public class BulletPoolHelper : MBSingleton<BulletPoolHelper>
{
    protected override void ChildAwake()
    {
    }
}
